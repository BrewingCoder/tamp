namespace Tamp;

/// <summary>
/// Built-in configuration enum for the common Debug/Release split. Build
/// authors are free to ignore this and use their own; it exists as a
/// convenience for the common case.
/// </summary>
public enum Configuration
{
    Debug,
    Release,
}
